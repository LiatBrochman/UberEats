import { Text, View, StyleSheet, Image } from 'react-native';


const DishListItem = () => {
    return (
    <View>
        <Text>DishListItem</Text>
    </View>
    );
};

const style = StyleSheet.create({})
export default DishListItem;
